<header>
  <nav>
    <div class="navbar navbar-inverse navbar-fixed-top hidden-phone">
      <div class="navbar-inner">
        <div class="container">
          <ul class="nav pull-left">
            <li id="logo">
              <%= t('PCB')  %>
            </li>
            </ul>
            <ul class="nav pull-right">
              <li><a href="/"><i class="icon-home icon-white"> </i> <%= t('home') %></a></li>
              <% if signed_in? %>
              <li><%= link_to "Users", users_path %></li>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <%= t('account') %> <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to t('profile'), current_user %></li>
                  <li><%= link_to t('settings'), edit_user_path(current_user) %></li>
                  <li class="divider"></li>
                  <li>
                    <%= link_to t('signout'), signout_path, method: "delete" %>
                  </li>
                </ul>
              </li>
              <% else %>
              <li><%= link_to t('signin'), signin_path %></li>
              <li><a href="/signup" class="torange">注册</a></li>
              <% end %>
            </ul>
        </div>
      </div>
    </div>
  </nav>
</header>
